dogbot_arm_controller:
  servo_gripper_name:
    {
      type: string,
      default_value: "servo_gripper_joint",
      description: "Name of the arm gripper's joint",
    }
  servo_shoulder_name:
    {
      type: string,
      default_value: "servo_shoulder_joint",
      description: "Name of the arm shoulder's joint",
    }
  servo_forearm_name:
    {
      type: string,
      default_value: "servo_forearm_joint",
      description: "Name of the arm forearm's joint",
    }